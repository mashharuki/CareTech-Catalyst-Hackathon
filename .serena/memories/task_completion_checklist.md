# Task Completion Checklist
- Confirm requested files were updated in both `.codex/skills` and `.agents/skills` when mirrored skills exist.
- Run `pnpm run format` if code-style-sensitive files were edited.
- Verify changes with `git diff` and `git status`.
- Summarize what changed, why, and any follow-up actions in Japanese.